#Week 1)
#  - Lists, maps
#  - Conditions
#  - Recursion
#  - Tail recursion

defmodule Week1 do

  # Return longer string
  def take_longer(left, right) do
    nil
  end
  
  def fibonacci(i) do
    nil
  end

  # https://en.wikipedia.org/wiki/Greatest_common_divisor#Calculation
  def euclid_gcd(i) do
    nil
  end

  def is_prime(i) do
    nil
  end

  # Find some way to test for correctness
  def tailrec_fibonacci(i) do
    nil
  end

  # if number is even, next is divided by two
  # if number is odd, next is 3 * n  + 1 
  def collatz_seq(i) do
    nil
  end
end