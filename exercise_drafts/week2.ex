#Week 2)
#  - High order functions
#  - Pattern match
#  - Lambda functions
#  - Functions accepting functions

defmodule Week2 do
  
  # Just upcase everything
  def lists_to_upcase(i) do
    nil
  end

  # Some sort of objects
  def extract_names(i) do
    nil
  end

  # Merge two lists with their indicise, list of tuples {index, left,right}
  def merge_with_indices(left, right) do
    nil
  end
  
  # Reverse list using fold
  def fold_list_backwords(xs) do
    nil
  end

  # Map of animals as input, filter :type carnivore
  def carnivores_only(i) do
    nil
  end

  # Find the average of a list
  def average_of_list(xs) do
    nil
  end

  # Square sum of a non-linear list
  def square_sum_of_list(xs) do
    nil
  end

  # Return the binomial coefficient
  # B(n, m) = B(n-1, m) + B(n-1, m-1)
  # B(n, 0) = 1
  # B(0, m) = 0 when k > 0

  def binomial(n, m) do
    nil
  end

  # Ah the lovely quicksort
  def quicksort(xs) do
    []
  end

end