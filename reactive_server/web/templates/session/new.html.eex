<h2>Login</h2>

New user? Try to <a href="<%= user_path(@conn, :new) %>">create an account</a> first.

<%= render "form.html", changeset: @changeset,
                        action: login_path(@conn, :create) %>